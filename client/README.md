# Lost & Found React Client

## Development

Install dependencies:
```bash
npm install
```

Run development server:
```bash
npm run dev
```

The dev server runs on http://localhost:5173 with proxy to Flask backend on :8080

## Build

Build for production:
```bash
npm run build
```

This will output to `../server/static/` for Flask to serve.

